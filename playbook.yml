- hosts: demo
  gather_facts: yes
  become: yes

  tasks:
    - name: Install Apache
      yum:
        name: httpd
        state: present
        use_backend: yum

    - name: Start Apache
      service:
        name: httpd
        state: started
